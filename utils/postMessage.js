/* eslint-disable no-console*/
import colors from 'colors';

console.log('Your app has been compiled in production mode and written to /dist.'.green);
console.log('Type "npm run prod" to start production server.'.yellow);
